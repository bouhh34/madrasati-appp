# Audit — 14 September 2026

Scope: `bouhh34/madrasati-appp`, `main`, baseline `89430155a10c123c6baaa50264dd1a2d0e16a270`. Existing Fastify/PostgreSQL application and vanilla Arabic/French frontend retained.

| Status | Area | Evidence / required work |
| --- | --- | --- |
| ✅ | Deployment | Render service `madrasati-appp`, same repository and branch; baseline deployment live. |
| ✅ | Authentication foundation | Argon2id, pepper, hashed server sessions, HttpOnly cookies, CSRF, throttling, security versions and step-up exist. Existing credentials must be retained. |
| ✅ | Academic formula | Existing implementation and test use school-configured term weights, default 1:2:3; preserve this formula. |
| 🟡 | Core management | Create/update classes, subjects, students and accounts exist. Search, assignment UI, safe archive and year context incomplete. |
| 🟡 | Attendance / exams | Individual attendance and exam scheduling exist. Atomic bulk attendance, summaries and links from exams to grades missing. |
| ⚪ | Academic years | School has a year label only. Classes and academic data have no year identity. |
| ⚪ | Class curriculum | Subjects are school-wide; no explicit class-subject relation. |
| ⚪ | Results / ranking | Per-student averages exist, but no complete class results, ties or completeness-aware ranking. |
| 🔴 | Reproducible installation | `npm ci` fails: qrcode and transitive dependencies missing from lockfile. |
| 🔴 | Existing tests | Baseline: 14 pass, 2 fail, 1 database test skipped. No CI workflow actually present despite README claim. |
| ⚠️ 🔐 | Disabled school | Session loading and school selection do not consistently require an active school. Permission functions do not consistently require an active membership. |
| ⚠️ | Destructive delete | Platform school DELETE cascades to memberships, grades and other records. Replace with recoverable archive. |
| ⚠️ | Audit integrity | Teacher cannot read preceding audit hash under existing RLS, interrupting hash chain. |
| ⚠️ | Recovery | Forgot-password generates a token without a delivery channel. Director recovery exists, with insufficient target checks. |
| ⚠️ | Validation | Empty numeric input coerces to zero; dates only regex-checked; subject scale changes can reinterpret stored grades. |
| 🔐 | Tenant enforcement | Composite keys and forced RLS exist. Need real role and IDOR HTTP tests using two schools and non-superuser DB role. |
| 🎨 | Frontend architecture | Duplicate function declarations, appended IIFEs, inaccessible Super Admin loader, inline styles/scripts conflicting with CSP. |
| 📱 | Mobile / RTL | Responsive base exists; growing bottom nav, fixed-width tables and inconsistent dynamic translation need work. |
| 🖨️ | Reports | Single-student print dialog exists. No reliable two-per-A4 bulk layout; styles are injected inline; missing-subject completeness absent. |
| 🔐 | Offline cache | Service worker caches arbitrary navigation including report-verification pages containing student data. Restrict to explicit static shell. |

Production database metadata query via Render failed with TLS/EOF; no database settings, secrets or live records changed. Runtime logs show earlier school/director SQL errors before the latest baseline commit. These are historical evidence, not proof the latest code still fails identically.

Implementation order: stability/security; years and core relationships; bulk grades/results; attendance/exams; reports; consistent mobile RTL/French UI; end-to-end and production verification.
